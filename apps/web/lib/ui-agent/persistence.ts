import type { UIFinding, LayoutFix } from "./types"; export interface UIHistory{findings:UIFinding[];fixes:LayoutFix[];inspected:Record<string,number>}; const key="marginalia-ui-guardian-history-v1"; export function loadUIHistory():UIHistory{if(typeof window==="undefined")return{findings:[],fixes:[],inspected:{}};try{return JSON.parse(window.localStorage.getItem(key)??"null")??{findings:[],fixes:[],inspected:{}}}catch{return{findings:[],fixes:[],inspected:{}}}} export function saveUIHistory(value:UIHistory){try{if(typeof window!=="undefined")window.localStorage.setItem(key,JSON.stringify(value))}catch{}}
